services:
  - type: web
    name: beginneruser-app
    env: python
    plan: free
    buildCommand: pip install --upgrade pip setuptools wheel && pip install -r requirements.txt
    startCommand: bash -c "streamlit run Dashboard.py --server.port $PORT"
    autoDeploy: true
